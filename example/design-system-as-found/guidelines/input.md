# Input

A labelled text field. The label is required — do not use a placeholder instead.

## Import

```tsx
import { Input } from "@acme/ui/input";
```

## Usage

```tsx
<Input label="Email" hint="We only use this for receipts." isInvalid={false} />
```
