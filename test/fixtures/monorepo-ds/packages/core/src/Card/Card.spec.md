# Card

A surface that groups things that belong together.

```tsx
import { Card } from "@acme/mono-ui";

<Card>Anything that reads as one unit.</Card>;
```

## Accessibility

A card is a `section`. Give it a heading, or it is a box a screen reader walks
past without announcing.
