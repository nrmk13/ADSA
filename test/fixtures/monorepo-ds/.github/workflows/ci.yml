name: ci
on: [push, pull_request]
jobs:
    verify:
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v4
            - run: pnpm test
            - run: pnpm lint
            - run: pnpm check:docs
            - run: pnpm test:axe
